function setup() {
  createCanvas(600, 400);
  
}

function draw() {
  background(250 ,250 ,100);
  
  
  fill(250,250,200);
  ellipse(mouseX,mouseY,200,200);
  
  
  fill(200,250,200);
  rect(400,100,200,150);
}
